# Supabase — projeto "fluxo-crm"
# Nunca commitar o .env.local real. A service role key só é usada pelo script
# de seed (rodado localmente), jamais pelo runtime da aplicação.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Apenas para supabase/seed/seed.ts (local, nunca no deploy)
SUPABASE_SERVICE_ROLE_KEY=
