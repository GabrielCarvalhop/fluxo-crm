import { PlaceholderPage } from "@/components/shared/placeholder-page";
import { Settings } from "lucide-react";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      icon={Settings}
      title="Configurações"
      description="Perfil, tags, origens, segmentos, templates, checklists, motivos de perda e estágios do pipeline — chega na Fatia 5."
    />
  );
}
