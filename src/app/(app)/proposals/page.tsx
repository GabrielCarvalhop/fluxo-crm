import { PlaceholderPage } from "@/components/shared/placeholder-page";
import { FileText } from "lucide-react";

export default function ProposalsPage() {
  return (
    <PlaceholderPage
      icon={FileText}
      title="Propostas"
      description="Rascunho até aceita/recusada, com valor proposto e taxa de fechamento — chega na Fatia 4."
    />
  );
}
